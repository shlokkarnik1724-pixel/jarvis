# Optional — without this key, extraction & simulator use a high-quality demo engine
OPENAI_API_KEY=

# Session signing secret (change in production)
AUTH_SECRET=tactix-dev-secret-change-me

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
